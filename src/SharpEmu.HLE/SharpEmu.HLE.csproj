<!--
Copyright (C) 2026 SharpEmu Emulator Project
SPDX-License-Identifier: GPL-2.0-or-later
-->

<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <PropertyGroup>
    <NoWarn>$(NoWarn);1591</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="SharpEmu.Core" />
    <InternalsVisibleTo Include="SharpEmu.Libs.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SharpEmu.Logging\SharpEmu.Logging.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ppy.SDL3-CS" />
  </ItemGroup>

  <!-- aerolib.bin (the runtime NID -> name catalog) is derived data: generated here
       from scripts/ps5_names.txt at build time and never committed. Replaces
       scripts/generate_aerolib_binary.py.

       The generator task is compiled from local source (build/AerolibBinaryGenerator.build.cs)
       by RoslynCodeTaskFactory into a temporary task assembly. It deliberately does NOT
       load SharpEmu.SourceGenerators.dll: that earlier design read the freshly built
       analyzer assembly in a separate TaskHostFactory process, and on the Windows host
       building over the \\wsl.localhost 9P share that cross-process read of a just-written
       project output was intermittently incoherent — failing with MSB4062
       "GenerateAerolibBinaryTask could not be loaded" and cascading into a missing
       SharpEmu.HLE reference assembly for downstream projects. Compiling from local source
       removes the cross-project/cross-process dependency (and the SourceGenerators
       ProjectReference HLE previously carried only for build ordering) entirely. -->
  <PropertyGroup>
    <SharpEmuAerolibNamesFile>$(MSBuildThisFileDirectory)..\..\scripts\ps5_names.txt</SharpEmuAerolibNamesFile>
    <SharpEmuAerolibTaskSource>$(MSBuildThisFileDirectory)build\AerolibBinaryGenerator.build.cs</SharpEmuAerolibTaskSource>
  </PropertyGroup>

  <!-- Build-time-only source: fed to RoslynCodeTaskFactory below, never compiled into
       SharpEmu.HLE (it references Microsoft.Build.Framework, which the runtime assembly
       does not). -->
  <ItemGroup>
    <Compile Remove="build\**\*.cs" />
  </ItemGroup>

  <UsingTask TaskName="SharpEmu.HLE.Build.GenerateAerolibBinaryTask"
             TaskFactory="RoslynCodeTaskFactory"
             AssemblyFile="$(MSBuildToolsPath)\Microsoft.Build.Tasks.Core.dll">
    <Task>
      <Code Type="Class" Language="cs" Source="$(SharpEmuAerolibTaskSource)" />
    </Task>
  </UsingTask>

  <!-- Generates aerolib.bin before resource processing embeds it. Skipped for
       design-time builds, which do not compile and so never need the embedded catalog.
       Incremental via Inputs/Outputs (names file + the generator source). -->
  <Target Name="GenerateAerolibBinary"
          Inputs="$(SharpEmuAerolibNamesFile);$(SharpEmuAerolibTaskSource)"
          Outputs="$(IntermediateOutputPath)aerolib.bin">
    <GenerateAerolibBinaryTask NamesFile="$(SharpEmuAerolibNamesFile)"
                               OutputFile="$(IntermediateOutputPath)aerolib.bin" />
  </Target>

  <!-- The resource item is created inside a target (not statically) so design-time
       builds never reference a file that was not generated, and in a separate target
       from the generation so an up-to-date skip of GenerateAerolibBinary cannot drop
       the item (an up-to-date target skips its whole body, ItemGroups included).
       Hooked before AssignTargetPaths: dynamic EmbeddedResource items added later
       than that miss the resource pipeline entirely. -->
  <Target Name="EmbedAerolibBinary"
          BeforeTargets="AssignTargetPaths"
          DependsOnTargets="GenerateAerolibBinary"
          Condition="'$(DesignTimeBuild)' != 'true'">
    <ItemGroup>
      <EmbeddedResource Include="$(IntermediateOutputPath)aerolib.bin"
                        LogicalName="SharpEmu.HLE.Aerolib.aerolib.bin"
                        Visible="false" />
    </ItemGroup>
  </Target>
</Project>
